<!--
    The widget's copy of DuskReadIcons.Play — same geometry, same 2.4 weight and
    round terminals. RemoteViews cannot render a Compose ImageVector, so the
    set has to exist twice; see ic_widget_paste.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">

    <path
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="2.4"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:pathData="M8.5,5.5 L18.5,12 L8.5,18.5 Z" />
</vector>
